# Application Name
spring.application.name=auth-service

# Server Port
server.port=8071

# MySQL DB Configuration
spring.datasource.url=jdbc:mysql://localhost:3307/myfin_auth_db
spring.datasource.username=root
spring.datasource.password=007sj007

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Eureka Client Configuration
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true

# JWT Secret - must match API Gateway secret
jwt.secret=myfinbank_super_secret_key_2026_myfinbank_secure_token_key
jwt.expiration=86400000

# Logging
logging.level.org.springframework.security=INFO